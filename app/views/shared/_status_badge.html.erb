<%# locals: (status:, label: nil) %>
<span class="inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-semibold ring-1 ring-inset <%= status_color(status) %>">
  <%= label || status.to_s.humanize.titleize %>
</span>
